services:
  image-generator:
    build: 
      context: .
      dockerfile: Dockerfile
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./output:/app/output
      - huggingface_cache:/root/.cache/huggingface

volumes:
  huggingface_cache:
